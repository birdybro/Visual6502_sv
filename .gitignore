# Verilator build outputs
sim/verilator/obj_dir/
**/obj_dir/
*.vcd
*.fst

# Editor / OS detritus
*.swp
*.swo
.DS_Store

# Trace artifacts: keep reference TSVs under tests/traces/ in git
# (they are golden baselines), but ignore transient log files.
tests/traces/*.log
tests/traces/rtl_*.tsv

# Build outputs
build/
*.o
*.a
